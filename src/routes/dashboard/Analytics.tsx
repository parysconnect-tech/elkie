import { PagePlaceholder } from '@/components/PagePlaceholder'

export default function DashboardAnalytics() {
  return (
    <PagePlaceholder
      title="Your traffic"
      description="Page views (last 7 / 30 days), top pages, referrers — pulled from Supabase."
      step="step 9"
    />
  )
}
