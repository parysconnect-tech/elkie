# =====================================================================
# Elkie Web Studio — Environment Variables
# Copy this file to `.env.local` and fill in the values you have.
# Anything left blank just disables the related feature — the app
# still runs end-to-end with stubs.
# =====================================================================

# ---------- Supabase (auth, database, realtime) ----------
# Get these from https://supabase.com → your project → Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Service role key — server-side only (Edge Functions). NEVER expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# ---------- Stripe (payments — use TEST keys for now) ----------
# Get these from https://dashboard.stripe.com → Developers → API keys
# The browser only needs the publishable key (just to know Stripe is "on").
VITE_STRIPE_PUBLISHABLE_KEY=

# These live as Supabase Edge Function secrets, NOT in the browser bundle:
#   supabase secrets set STRIPE_SECRET_KEY=sk_test_... --project-ref YOUR-REF
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Stripe recurring Price IDs (create products in Stripe → copy price IDs).
# These are read by the create-checkout-session Edge Function.
STRIPE_PRICE_STARTER_MONTHLY=
STRIPE_PRICE_STARTER_ANNUAL=
STRIPE_PRICE_CUSTOM_MONTHLY=
STRIPE_PRICE_CUSTOM_ANNUAL=
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_PRO_ANNUAL=

# Optional one-time setup-fee Price IDs (added to the first invoice).
# Leave blank to skip charging a setup fee.
STRIPE_PRICE_STARTER_SETUP=
STRIPE_PRICE_CUSTOM_SETUP=
STRIPE_PRICE_PRO_SETUP=

# ---------- Resend (transactional email, called from Supabase Edge Functions) ----------
# Get from https://resend.com → API Keys
RESEND_API_KEY=
RESEND_FROM_EMAIL=hello@elkie.com
RESEND_FROM_NAME=Elkie Web Studio

# Where to send new lead notifications
ADMIN_NOTIFICATION_EMAIL=

# ---------- Cloudflare Turnstile (invisible CAPTCHA on lead form + signup) ----------
# Get from https://dash.cloudflare.com → Turnstile
VITE_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# ---------- Google Analytics ----------
VITE_GA_MEASUREMENT_ID=G-1CG236C3N9

# ---------- WhatsApp (floating button) ----------
VITE_WHATSAPP_NUMBER=
